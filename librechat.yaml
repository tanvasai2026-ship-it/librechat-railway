version: 1.3.1

interface:
  customWelcome: "Welcome to your private AI workspace. Choose OpenRouter, select a model, and enable Web Search when you need current information."

  modelSelect: true
  parameters: true
  presets: true
  prompts: true
  bookmarks: true
  temporaryChat: true
  contextUsage: true
  contextCost: true

  webSearch: true
  fileSearch: true
  fileCitations: true
  maxCitations: 50
  minRelevanceScore: 0.0
  runCode: true

  privacyPolicy:
    externalUrl: "https://librechat.ai/privacy-policy"
    openNewTab: true

  termsOfService:
    externalUrl: "https://librechat.ai/tos"
    openNewTab: true
    modalAcceptance: true
    modalTitle: "Terms of Service for My Private LibreChat"
    modalContent: |
      # Terms and Conditions

      This is a private, personal LibreChat service.

      - Do not share access credentials or API keys.
      - AI-generated output may be inaccurate. Verify important legal, medical, financial, tax, compliance, and other high-impact information with qualified sources.
      - You are responsible for activity performed through your account and for third-party API usage charges.

endpoints:
  custom:
    - name: "OpenRouter"
      apiKey: "${OPENROUTER_API_KEY}"
      baseURL: "https://openrouter.ai/api/v1"
      models:
        fetch: true
        default:
          - "openai/gpt-4o-mini"
          - "google/gemini-2.5-flash"
          - "qwen/qwen3-32b"
          - "anthropic/claude-sonnet-4"
      titleConvo: true
      titleModel: "openai/gpt-4o-mini"
      summarize: false
      modelDisplayLabel: "OpenRouter"

webSearch:
  searchProvider: "tavily"
  scraperProvider: "tavily"
  tavilyApiKey: "${TAVILY_API_KEY}"
  logResult: false
  tavilySearchOptions:
    searchDepth: "advanced"
    maxResults: 10
